#include<stdio.h>
void main()
{
	int side;
	scanf("%d",&side);
	printf("area of a square with side %d is %d\n",side,side*side);
	printf("perimeter of a square with side %d is %d\n",side,4*side);
}
